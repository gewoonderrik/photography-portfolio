{% if pagination.pages %}
  {% spaceless %}
    <nav class="pagination">
      {% if pagination.prev %}
        <a class="prev" href="{{ pagination.prev.link }}">
          Previous
        </a>
      {% endif %}

      {% for page in pagination.pages %}
        {% if page.link %}
          <a class="{{ page.class }}" href="{{ page.link }}">
            {{ page.title }}
          </a>
        {% else %}
          <span class="{{ page.class }}">
            {{ page.title }}
          </span>
        {% endif %}
      {% endfor %}

      {% if pagination.next %}
        <a class="next" href="{{ pagination.next.link }}">
          Next
        </a>
      {% endif %}
    </nav>
  {% endspaceless %}
{% endif %}
